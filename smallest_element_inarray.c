#include<stdio.h>
int main()
{
    int a[25];
    int i,small,pos;
    printf("enter the integers:");
    scanf("%d",&a[i]);
    for(i=0;i<a[i];i++)
    {
        if(i==0||small>a[i]);
        {
            small=a[i];
            pos=i+1;

        }
    }
printf("smallest element in array: %d,at position: %d\n",small,pos);
return 0;
}